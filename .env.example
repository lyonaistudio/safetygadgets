# Copiez ce fichier en `.env` et remplissez avec votre clé Stripe (mode test
# pour commencer : dashboard Stripe > Developers > API keys > Secret key).
# Ne jamais commettre le fichier `.env` réel (déjà exclu via .gitignore).
STRIPE_SECRET_KEY=sk_test_REPLACE_ME
